import { SurroundDirective } from './surround.directive';

describe('SurroundDirective', () => {
  it('should create an instance', () => {
    const directive = new SurroundDirective();
    expect(directive).toBeTruthy();
  });
});
