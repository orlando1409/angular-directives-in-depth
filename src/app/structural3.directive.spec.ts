import { Structural3Directive } from './structural3.directive';

describe('Structural3Directive', () => {
  it('should create an instance', () => {
    const directive = new Structural3Directive();
    expect(directive).toBeTruthy();
  });
});
