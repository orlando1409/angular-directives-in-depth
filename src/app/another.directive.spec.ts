import { AnotherDirective } from './another.directive';

describe('AnotherDirective', () => {
  it('should create an instance', () => {
    const directive = new AnotherDirective();
    expect(directive).toBeTruthy();
  });
});
