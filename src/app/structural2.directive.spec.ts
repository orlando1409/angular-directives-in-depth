import { Structural2Directive } from './structural2.directive';

describe('Structural2Directive', () => {
  it('should create an instance', () => {
    const directive = new Structural2Directive();
    expect(directive).toBeTruthy();
  });
});
